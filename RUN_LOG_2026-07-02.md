# Run Log — 2026-07-02 (GPU2 / resco)

**Purpose:** operational tracker for today's execution, mapped back to `ANALYSIS_PLAN.md` items so nothing gets lost in the output pile. Update this file as jobs finish; treat `ANALYSIS_PLAN.md` as the source of truth for what each item *means*, this file as the source of truth for *where things stand right now*. Actual numeric results for manuscript revision live in `MANUSCRIPT_RESULTS_LOG.md`; the new L2 diagnostic's implementation spec lives in `L2_CRITIC_DIAGNOSTIC_DESIGN.md` — not here.

---

## ⚠️ Resolved / verified today

- [x] **D_G methodology check — RESOLVED.** `analysis_dg_flow_k4`/`analysis_dg_metis_k4` failed loudly with a state-dict mismatch (checkpoint had global `attention_layers.*`, loader expected `partitioned_attention.*`) — no invalid output produced, empty dirs removed. Headline Ψ(P,D_G) uses the already-computed comparison: same global matrix (`ing21_colight_original_seed7_gpu2_refire_dg/model_dependency.npz`) with InfoMap/METIS partitions applied → `psi/ingolstadt21_D_G_colight_original_seed7_gpu2/model_dependency_psi.csv`. Guard + README added to `analysis/outputs/model_dependency/` so future outputs self-mark headline-valid vs. diagnostic-only.
- [x] **WandB check — Ingolstadt21 CoLight mechanism.** Confirmed via direct `mcp__wandb` query: no I21 CoLight run has `colight_partitioned_message_passing` set. The `flow_k4`/`metis_k4` pair (created 2026-05-26 23:33/23:34, finished, seed 67) restricts the road graph via `signal_yaml`, not the Appendix's documented Variant A flag. An earlier, distinct generation (`flow_partition`/`metis_partition`/`default_fixed_topology`, created 03:12–08:16 same day, 5-module management, no `signal_yaml`) also exists. **Still open:** confirm which generation produced the numbers currently in the Appendix's `i21_colight` table, and correct the Appendix's Variant A description to match the mechanism actually used.
- [x] **WandB check — Cologne8 CoLight mechanism (new, this turn).** `cologne8+colight+original_no_partition+seed7_gpu1` has `colight_partitioned_message_passing: False` explicitly set — confirmed genuinely global, valid D_G source. `cologne8+colightregional+flow_k3+seed7_gpu1` / `metis_k3` are algorithm=`CoLightRegional` (**Variant B** — separate independent learner/replay buffer per region, not a shared model). D_G cannot be extracted from these at all — there is no unified computational graph spanning modules, not merely a masked one. Only the no-partition checkpoint is usable as a D_G source, same rule as I21.
- [x] Confirmed `state_diagnostics_summary.csv` contains Ψ(P,D_MI) and Spearman ρ(W,D_MI) directly (`mean_psi_d_mi`, `mean_rho_flow_d_mi` columns).
- [x] **Full-state Ingolstadt21 D_MI — now complete** (was the CPU-heavy queued item). Output: `analysis/outputs/state_diagnostics_full_state/state_diagnostics_summary.csv`. See `MANUSCRIPT_RESULTS_LOG.md` for the numbers and what changed vs. the `queue_total` proxy.
- [ ] H2 spill-back containment — **ran, but underpowered (user-confirmed, sparse events on both networks; literally zero on Cologne8).** Not resolved — see Pending below.

---

## ✅ Artifacts on disk (per user's inventory this turn)

| Output | Path | ANALYSIS_PLAN item |
|---|---|---|
| Run inventory | `analysis/outputs/results/run_inventory.csv` | Tier 1 #3 infra |
| Result summary (eval-curve based, **not yet normalized**) | `analysis/outputs/results/result_summary.csv` | Tier 1 #3 — pending `testing_metrics.json` |
| Learning curves / AUC-LC inputs | `analysis/outputs/results/learning_curves.csv` | Tier 1 #3 |
| Normalization commands | `analysis/outputs/results/normalization_commands.json` | Tier 1 #3 infra |
| Φ(P,W) flow retention, C8 + I21 | `analysis/outputs/flow_retention/flow_retention.csv` | Tier 3 #8a — done |
| Warm-up traces, C8 + I21, seeds 0–2 | `analysis/outputs/traces/` | Tier 1 #1 prerequisite — done |
| State diagnostics (C8 full-state, I21 `queue_total` proxy) | `analysis/outputs/state_diagnostics/state_diagnostics_summary.csv` | Tier 1 #1, #2 (D^MI leg), Tier 3 #8b |
| State diagnostics, I21 **full-state** (supersedes proxy) | `analysis/outputs/state_diagnostics_full_state/state_diagnostics_summary.csv` | same, upgraded |
| CoLight global D_G (I21, valid) | `analysis/outputs/model_dependency/ing21_colight_original_seed7_gpu2_refire_dg/model_dependency.npz` + `influence_summary.json` | Tier 1 #2 (D^G leg) |
| Ψ(P,D_G) headline, I21 | `analysis/outputs/model_dependency/psi/ingolstadt21_D_G_colight_original_seed7_gpu2/model_dependency_psi.csv` | Tier 1 #2 — done for I21 |
| CoLight global D_G (C8, valid) | `analysis/outputs/model_dependency/cologne8_colight_original_seed7_global_dg/model_dependency.npz` | Tier 1 #2 (D^G leg) — **done, replicates I21's k-reversal** |
| Ψ(P,D_G) headline, C8 | `analysis/outputs/model_dependency/psi/cologne8_D_G_colight_original_seed7/model_dependency_psi.csv` | Tier 1 #2 — done for C8, see `MANUSCRIPT_RESULTS_LOG.md` §5 |
| Model-dependency README (headline-valid vs diagnostic-only guard) | `analysis/outputs/model_dependency/README.md` | hygiene |

## 🔄 Currently running

| Job | What it does | ANALYSIS_PLAN item |
|---|---|---|
| `analysis_gpu2_normalize` | Normalizing completed MAPPO/IPPO runs via `evaluate.py ... gpu:2` → produces `testing_metrics.json` | Tier 1 #3 |
| `analysis_gpu2_queue` (currently: `ing21_ippo_original_local_seed0_gpu2_refire`) | Refire queue — fills missing I21 IPPO no-partition baseline; queued next: standard CoLight `flow_k4`/`metis_k4` seed 7 | Tier 1 #3, Open Question #3 |
| Also running (from before) | I21 CoLight original/no-partition GPU2 refire; I21 CoLightRegional flow/metis k4/k5 GPU2 refires; Grid4x4 CoLight/CoLightRegional GPU2 jobs | Tier 1 #3 (I21, Grid4x4 scale-up) |

## ⏭ Next, in priority order

1. **[DONE 2026-07-02] Ψ(P,D_G) for Cologne8 computed.** Result: flow wins at k=3, METIS wins at k=5 — **replicates I21's k-dependent reversal on a second network.** Full numbers and analysis in `MANUSCRIPT_RESULTS_LOG.md` §5. This is now a real, cross-network finding requiring explicit manuscript treatment, not an anomaly to explain away.
2. **[New, recommended] Cologne8 CoLight performance at k=5** (only k=3 currently exists). This is the cheap way to check whether the Ψ(D_G) reversal actually predicts a performance reversal — the one existing check (I21 Variant B, k=5) shows performance *not* reversing despite Ψ(D_G) reversing, which is unresolved. See `MANUSCRIPT_RESULTS_LOG.md` §5/§Open items #6.
3. **After `analysis_gpu2_normalize` finishes:** `conda run -n resco python -m analysis.results summarize` → refreshes `result_summary.csv` with real `testing_metrics.json` → Tier 1 #3.
4. **After refires complete:** evaluate/summarize IPPO local seed0, CoLight flow-k4 seed7, CoLight metis-k4 seed7 → closes Tier 1 #3 baseline gaps, answers Open Question #3 (does L3 turn around on I21 or not).
5. **H1/H2 gate reliability (cross-network, not resolved by either network's data alone).** Full-state fixed I21's *direction* problem but effect size is still weak (see `MANUSCRIPT_RESULTS_LOG.md`); H2 is essentially unmeasurable with current warm-up-trace extraction. Needs either longer/stress traces (already flagged as pending) or an honest reframing of H1/H2 as diagnostic rather than a hard pass/fail gate in the manuscript text — this decision doesn't depend on which network.

## 🚫 Not runnable yet

- ~~Paper-grade D_Q~~ — **resolved to a design decision (2026-07-02).** Code read confirmed the MAPPO global critic gives every output row an identical input (no target index `i`); true pairwise D^Q is architecturally impossible from existing checkpoints, not a rerun issue. Reduced diagnostic designed instead (`g_j` source-sensitivity + `Γ(P,g)` Shannon-entropy concentration) — full spec in `L2_CRITIC_DIAGNOSTIC_DESIGN.md`. **This is now an implementation task (user revising code), not an analysis-tooling blocker.** True target-conditioned D^Q (needs a decomposed-head critic + retraining) remains separately deferred.
- **Paper-grade H2** — pending longer/stress traces; current extraction under-detects events on both networks (zero on C8).
- **Regional-checkpoint attention entropy** — partially supported by code, not yet cleanly produced.

---

## Today's honest bottom line

Complete for both networks: Tier 1 #2 D^MI and D^G legs, Tier 3 #8a/#8b. Complete for I21: Tier 1 #1 (full-state, direction fixed, still statistically weak). Complete for C8: Tier 1 #1 (H1 gate mostly failing, H2 empty). **Headline new result: Ψ(D_G)'s k-dependent reversal (small k favors flow, larger k favors METIS) is now confirmed on both networks independently — this is real, not an artifact, and needs explicit manuscript treatment (`MANUSCRIPT_RESULTS_LOG.md` §5).** Open tension: this diagnostic doesn't yet demonstrably predict actual trained-model performance (I21 Variant B k=5 performance doesn't reverse) — next check is C8 CoLight k=5 performance (Next #2). Still open regardless of network: Open Question #2 (IDQN), Open Question #4 (≥5-seed expansion — still 3 seeds for diagnostics, single-seed for performance), Tier 1 #4 (needs the k=5 performance check above), H2 (needs better instrumentation), and the Appendix's CoLight architecture description (needs correcting — I21 used `signal_yaml`-restricted base CoLight, C8 used `CoLightRegional`/Variant B, neither matches the documented flag-based Variant A uniformly). **D^Q is no longer an open blocker** — resolved to a finalized implementation design (`L2_CRITIC_DIAGNOSTIC_DESIGN.md`, Shannon-entropy Γ diagnostic); next action is the user implementing it in code, not further analysis-side investigation.
